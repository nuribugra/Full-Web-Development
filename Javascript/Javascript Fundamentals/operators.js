var result = 0;
a = 1;
b = 2;
c = 3; 

// Atama operatörleri

result += a // result = result + a
result -= a // result = result - a
result *= a // result = result * a
result /= a // result = result / a
result %= a // result = result % a



// Karşılaştırma operatörleri

result = (a == b) // eşit mi
result = (a != b) // eşit değil mi
result = (3 === "3") // değer ve tipleri aynı mı
result = (a > b) // büyük mü
result = (a < b) // küçük mü
result = (a >= b) // büyük veya eşit mi
result = (a <= b) // küçük veya eşit mi



console.log (result)